#include <stdio.h>

int main() {
    // Add two numbers
    int num1 = 5;
    int num2 = 7;
    int sum = num1 + num2;

    // Print the result
    printf("Sum of %d and %d is: %d\n", num1, num2, sum);

    return 0;
}
